

test: 1:
1..2    # UNDONE: parses incorrectly, generating Float(1.0), Range lexeme is truncated but still parses to Range()

tree1:  1..2    # UNDONE: parses incorrectly, generating Float(1.0), Range lexeme is truncated but still parses to Range()
    1  : BinOp(TK.RANGE, '.')
    2  :     Float(TK.FLOT, 1.0)
    3  :     Int(TK.INT, 2)
